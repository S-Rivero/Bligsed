 <body>
    <ul id="mensaje_contenedor">
      <li>{{db.connectionLimit}}</li>
      <li>{{db.host}}</li>
      <li>{{db.user}}</li>
      <li>{{db.password}}</li>
      <li>{{db.database}}</li>

      {{!-- {{#each msg}}
        
        <li class="plantilla_mensaje mensaje_emisor">
            <div class="creador_mensaje">{{user.Nombre}}</div>
            <div class="contenido_mensaje">
                <img src="/Alumnos/media/momo_sad2.jpg" alt="">
                Que sad prro :'v xdxdxdxd
            </div>
        </li>
                                          <div class="publicaciones_autor">
                                          <div class="autor_nombre">
                                              {{Nombre}}
                                          </div>
                                          <div class="autor_fecha">
                                              12/3/2022
                                          </div>
                                      </div>
                                      <div class="publicaciones_contenido">
                                          <div class="contenido_titulo">
                                              {{titulo}}
                                          </div>
                                          <div class="contenido_texto">
                                              {{descripcion}}
                                          </div>
                                          <div class="contenido_vermas">
                                              <a class="boton_vermas">Ver Mas</a>
                                          </div>
                                      </div>
    {{/each}} --}}

    </ul>

    <form action="/msg" method="post" target="iframe" onsubmit="send(this.msg_in.value)">
      <input type="text" id="msg_in" placeholder="Escribe un mensaje...">
      <input type="submit" id="btn" value="Enviar"/>
    </form>	
    <iframe name="iframe" id="frame" src=""
      style="height: 250px; width: 250px;">
    </iframe>
    
    <script>
      window.onload = function() {
        const iframe = document.getElementById("frame");
        const iDocument = iframe.contentDocument;
        const iBody = iDocument.body;
        iBody.innerHTML = 
          '<form id="iform" action="/msg" method="post" >              <input type="text" id="msg" value="" style="display:none;">              <input type="text" id="uname" value="{{user.Nombre}}" style="display:none;">              <input type="number" id="uid" value="{{user.id}}" style="display:none;">              <input type="number" id="cid" value="" style="display:none;">              <input type="number" id="dbc" value="{{db.connectionLimit}}" style="display:none;">              <input type="text" id="dbh" value="{{db.host}}" style="display:none;">              <input type="text" id="dbu" value="{{db.user}}" style="display:none;">              <input type="text" id="dbp" value="{{db.password}}" style="display:none;">              <input type="text" id="dbdb" value="{{db.database}}" style="display:none;">            </form>';
          
      };
      function send(data){
        console.log(data);
        const iframe = document.getElementById("frame");
        const iDocument = iframe.contentDocument;
        var msg = iDocument.getElementById('msg').value;
        msg = data;
        console.log(iframe);
        console.log(iDocument);
        console.log(msg);
        
      }
    </script>
  </body>